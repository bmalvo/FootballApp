export type PlayersType = {
    id: string,
    object: {
        
        id: string;
        Imię: string;
        Nazwisko: string;
        Drużyna: string;
    }
}

export type PlayerDto = {

    object: object;
}